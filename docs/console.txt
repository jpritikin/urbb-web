
  // Test the 6-arm alternating pattern
  star.testPulse('outerAlternating', 'expand', 6)

  // Test 3-arm tipAngle pulse
  star.testPulse('tipAngle', 'contract', 3)

  // Set to 5 arms with random pulse
  star.testPulse(undefined, undefined, 5)

  // Test on current arm count
  star.testPulse('inner', 'expand')
